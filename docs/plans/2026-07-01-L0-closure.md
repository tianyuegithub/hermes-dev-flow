# L0 闭环计划 · 2026-07-01

> 目标：从「你一句话」到「Gitea 上开好 PR、带证据、闸门通过」全回路跑通。
> 做完 L0 后，暂停讨论，开始 L1（容器化+k8s）。

## 已完成 ✅

| # | 模块 | 验证结果 |
|---|------|----------|
| 1 | `git_prepare.sh` | clone + branch + clean，绕过代理 ✅ |
| 2 | `state.py` | 6 状态机 + 转换校验 ✅ |
| 3 | `dev-run-worker` (Claude Code) | $0.035 / 80s / health endpoint ✅ |
| 4 | `dev-gate` | 六项防假绿核查 → 用户拍板 → DONE ✅ |

## 待执行 📋

### Phase 1: 补齐基础设施（15min）

| # | 任务 | 产出 | 验证标准 |
|---|------|------|----------|
| 1.1 | 修复 `state.py` list 命令吞异常 | 修补一行 | `state.py list` 不报错 |
| 1.2 | 三份契约 JSON Schema | `contracts/` 下 3 个 `.schema.json` | 可校验 input/output/escalate JSON |
| 1.3 | 清理测试任务 | 删 test-001/seam-test-001 工作副本 | 干净起点 |

### Phase 2: dev-intake · 意图识别（20min）

| # | 任务 | 产出 | 验证标准 |
|---|------|------|----------|
| 2.1 | 写 `dev-intake/SKILL.md` | 分类 + 风险 + 闸门 + 路由逻辑 | — |
| 2.2 | 写 `scripts/intake.py` | 辅助脚本：LLM 推理 → 任务计划 JSON | `echo "加一个 /health" | intake.py` 输出有效计划 |
| 2.3 | 端到端测 intake → state.init | Hermes 直接执行 intake → 建任务 | state.py list 看到新任务 |

### Phase 3: dev-integrate · 集成收口（15min）

| # | 任务 | 产出 | 验证标准 |
|---|------|------|----------|
| 3.1 | 写 `dev-integrate/SKILL.md` | push + 开 PR 流程 | — |
| 3.2 | 写 `scripts/gitea_pr.sh` | 调 Gitea API 创建 PR | curl Gitea API → 返回 PR URL |
| 3.3 | 实战：把 test-001 分支开成 PR | Gitea 上出现 PR | 浏览器可访问 PR 链接 |

### Phase 4: 逃生舱协议（20min）

| # | 任务 | 产出 | 验证标准 |
|---|------|------|----------|
| 4.1 | 更新 `dev-run-worker` 加逃生舱检测 | prompt 中注入"遇岔口写 escalate.json" | Claude Code 在不确定时写 escalate.json |
| 4.2 | 写 `dev-escalation/SKILL.md` | Hermes 检测 escalate → 转用户 → 写 decision → resume | — |
| 4.3 | 模拟逃生舱流程 | 手工构造 escalate.json → Hermes 处理 → resume agent | 完整 escalate→decision→resume 闭环 |

### Phase 5: Codex worker + 全回路验证（25min）

| # | 任务 | 产出 | 验证标准 |
|---|------|------|----------|
| 5.1 | 更新 `dev-run-worker` 支持 Codex | prompt 拼装 + `codex exec --full-auto` 调用 | Codex 完成一次小任务 |
| 5.2 | **全回路端到端测试** | 新任务从 intake → worker → gate → integrate | Gitea 上出现 PR，状态 DONE |

---

## 全回路最终形态

```
你: "deer-flow 加 POST /echo 接口"
        │
        ▼
   ┌──────────┐
   │ dev-intake│  Hermes 分类 → 任务计划（feature / medium risk / 方案+验证闸门）
   └────┬─────┘
        │ 确认
        ▼
   ┌──────────────┐
   │ git_prepare   │  clone + 分支 + clean
   └────┬─────────┘
        │
        ▼
   ┌──────────────┐
   │ dev-run-worker│  Claude Code -p / Codex exec（黑盒）
   │              │  → 产出 evidence + commit
   └────┬─────────┘
        │                  ┌─────────────┐
        │ (遇岔口) ──────→ │ 逃生舱       │ → 你拍板 → resume
        │                  └─────────────┘
        ▼
   ┌──────────┐
   │ dev-gate  │  六项防假绿核查 → 你拍板（通过/打回+意见）
   └────┬─────┘
        │ 通过
        ▼
   ┌──────────────┐
   │ dev-integrate │  push + Gitea API 开 PR
   └────┬─────────┘
        │
        ▼
   🎉  Gitea PR 就绪，state=DONE
```

## 执行纪律

- 每 Phase 做完立即验证，卡住就记下来、跳过、继续下一个
- 不再提前优化（比如为 Codex 做抽象层——先 copy-paste 两个 worker，L1 再统一）
- 时间盒：每 Phase 最多 30min，超时就简化范围
